/**
 * types/index.ts
 *
 * Central barrel export for all shared types.
 * Import types like: import type { UserProfile, LayoutOutletContext } from "@/types"
 */

export type { LayoutOutletContext } from "./layout";
export type { UserProfile } from "./user";
